meta {
  name: Search Contacts
  type: http
  seq: 2
}

get {
  url: {{app_url}}/api/contacts/search?query=doe
  body: none
  auth: bearer
}

params:query {
  query: doe
}

auth:bearer {
  token: {{auth_token}}
}
